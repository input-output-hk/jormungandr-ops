use nix

export NIXOPS_DEPLOYMENT=example
export AWS_ACCESS_KEY_ID=example
export PACKET_API_KEY=example
export PACKET_PROJECT_ID=example

export NIX_PATH="nixpkgs=$(nix eval '(import ./nix {}).path')"
